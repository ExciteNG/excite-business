export interface DisList {
  name: string;
  lastModified: string;
  location: string;
  category: string;
  performance: string;
  id: string;
}
export interface DashProps {
  width: number;
  title: string;
  matrix: number;
}
